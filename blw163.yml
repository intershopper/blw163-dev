blw163:
  webservices:
    oracleTaskServiceUrl: https://${SOASUITEHOSTNAME}/integration/services/TaskService/TaskServicePort
    oracleTaskQueryServiceUrl: https://${SOASUITEHOSTNAME}/integration/services/TaskQueryService/TaskQueryService
    bpmwFairExceptionSafeUrl: https://${SOASUITEHOSTNAME}/bpmw-fair-1.100/ExceptionSafeSLSB
  externaltask:
    timeout: 10
  assignment:
    group: Essen Team1
spring:
  datasource:
    jdbc-url: ${DATASOURCE_URL}
    url: ${DATASOURCE_URL}
    username: camunda
    password: camunda
    driver-class-name: org.postgresql.Driver

camunda:
  bpm:
    database:
      schema-update: true
      table-prefix: ""
      type: postgres
